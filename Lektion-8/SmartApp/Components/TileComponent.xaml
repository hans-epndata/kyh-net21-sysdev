<UserControl x:Class="SmartApp.Components.TileComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartApp.Components"
             mc:Ignorable="d" 
             >
    <Border Style="{StaticResource tile}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0 0 0 20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Off" Foreground="White" Margin="0 0 50 0" HorizontalAlignment="Left" VerticalAlignment="Center" />
                <CheckBox Grid.Column="1" Style="{StaticResource tileToggleButton}"  HorizontalAlignment="Right" VerticalAlignment="Center" />
            </Grid>

            <TextBlock Grid.Row="1" Text="&#xf8f4;" Style="{StaticResource tileIcon}"  />

        </Grid>
    </Border>
</UserControl>
